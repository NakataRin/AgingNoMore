# AgingNoMore
## Setup

Create a folder named `GameRefs` and copy the following files from the game:

* `Assembly-CSharp.dll`
* `UnityEngine.CoreModule.dll`
* `BepInEx.dll`
* `0Harmony.dll`

Project references expect these files to exist locally.
